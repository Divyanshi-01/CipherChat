# CipherChat

CipherChat is a browser-based encrypted messaging app with a dark, minimal UI and browser-managed identities.
It generates local key-based identities, keeps private keys in the browser, and relays only encrypted message data through the backend.
Built with Spring Boot, Thymeleaf, and WebCrypto for a simple end-to-end encrypted chat experience.
